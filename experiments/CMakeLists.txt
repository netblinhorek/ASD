create_executable_project(TableExperiments)
set_target_properties(TableExperiments PROPERTIES CXX_STANDARD 17 CXX_STANDARD_REQUIRED ON)
if(MSVC)
    target_compile_options(TableExperiments PRIVATE /std:c++17)
endif()
